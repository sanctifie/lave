# Inventaire Ansible — remplacer par l'IP de l'EC2 créée (sortie Terraform ou console).
# En plus grand : inventaire DYNAMIQUE aws_ec2 (les hôtes sont découverts par tags).
[runners]
runner-1 ansible_host=203.0.113.10 ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/mbolo-ci.pem
