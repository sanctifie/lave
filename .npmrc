# React Native / Expo exige un node_modules « plat » pour que l'autolinking
# Gradle résolve @react-native/gradle-plugin (sinon settings.gradle → android/null).
# hoisted rend aussi les @types atteignables (supprime les TS2742 de portabilité).
node-linker=hoisted
